-- Auto-sourced by Neovim when the plugin is added to the runtimepath.
-- This ensures commands and keymaps are registered regardless of whether
-- the user calls setup() explicitly.
require("gitgen")
